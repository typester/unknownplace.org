package template_dir;
use strict;
use warnings;

sub start {
    defined $blosxom::template_dir;
}

sub template {
    # original loader
    my $loader = $blosxom::template;

    sub {
        local $blosxom::datadir = $blosxom::template_dir;
        $loader->(@_);
    }
}

1;
